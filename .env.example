# Configuración del entorno de desarrollo

# URL de la API backend (opcional)
# En desarrollo, se usa automáticamente http://localhost:8080/api
# En producción, se usa /api (ruta relativa)
# VITE_API_URL=http://localhost:8080/api

# Puerto del servidor de desarrollo (opcional)
# VITE_PORT=5173

# Modo de desarrollo
# NODE_ENV=development
